import csv

with open('students.csv', 
            mode='w', encoding='utf-8', newline='') as file:
    writer = csv.DictWriter(file, ["name", "chinese",
                                    "english", "math",
                                    "history", "health"])
    print(type(writer))
    writer.writeheader()
    writer.writerows(students)

#file.close()
file.closed
